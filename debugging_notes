testConflationMapAccount:
testBlockwiseDeplNo:
testConflationMapStorage:
	they fail du to fail
	assertEquals(expectedNoLogs.get(txCounter), result.getLogs().size());
	in StateManagerTestValidator.java:

deployContractCWithCreate2:
	fails with
	assertEquals(toValidateStatus, resultStatus);
	in
	public void accept(Transaction transaction, TransactionProcessingResult result)
	of SmartContractTestValidator.java

